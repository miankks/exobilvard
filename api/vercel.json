{
    "version": 2,
    "builds": [
        {
            "src": "index.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
            { "src": "/chat", "dest": "/Chat" },
            { "src": "/[^.]+", "dest": "/", "status": 200 }
    ]
}
